#!/usr/bin/env node
'use strict';

module.exports = module = {
  // signal to run the script to update store products collections with All Nations products
  updateAllNationProducts: __dirname + '/' + '.all_nations_products_collection_changed.watch'
};
